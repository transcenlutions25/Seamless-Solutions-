{
  "version": 2,
  "builds": [
    { "src": "apps/web/next.config.*", "use": "@vercel/next" },
    { "src": "apps/web/package.json", "use": "@vercel/next" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "apps/web/$1" }
  ]
}
